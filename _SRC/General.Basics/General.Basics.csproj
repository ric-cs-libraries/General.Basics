<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>

		<MyPackageMajorVersion>$(MyGeneralBasics_MajorVersion)</MyPackageMajorVersion>
		<MyPackageMinorVersion>4</MyPackageMinorVersion>
		<MyPackagePatchVersion>0</MyPackagePatchVersion>

		<Description>
            v4.4.0 : 
                - Ajout de IdsGenerator.NewTimestampedGuid (unique string prefixed by current timeestamp in milliseconds)
                - Renommage de IdsGenerator.GetNextGuidId en IdsGenerator.NewGuid, et devient également static !
                - Pas d'incrém. numéro version majeure (pas 5), car maîtrisé... par ailleurs.
		</Description>

	</PropertyGroup>

	<Import Project="$(MyCommonCsProjSrcPath)/SetVersions.csproj" /><!-- Affectation de <PackageVersion> et <Version> -->

</Project>
