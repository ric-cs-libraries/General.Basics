<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>

		<MyPackageMajorVersion>$(MyGeneralBasics_MajorVersion)</MyPackageMajorVersion>
		<MyPackageMinorVersion>4</MyPackageMinorVersion>
		<MyPackagePatchVersion>0</MyPackagePatchVersion>

		<Description><!-- Mettre ici en 1 ligne (même longue), le descriptif des nouveautés, pour cette nouvelle version du Package Nuget -->
			IEnumerableExtension (CheckAreValidIndexes_, ToChunks_, ToString_) - IListExtension (InsertAt_,RemoveAt_) - IntExtension (IsBetween_, IsInRange_, IsDivisibleBy_, IsPair_, IsOdd_) - RANDOMIZERS: IListExtension (Rename IntsTuplesGenerator to RandomIntsPairsGenerator) - Shufflers, (IIntsPairsGenerator, ComputedIntsPairsGenerator).  IntervalBase, IntsInterval, ValueShouldBeLowerOrEqualToException.
		</Description>

	</PropertyGroup>

	<Import Project="$(MyCommonCsProjSrcPath)/SetVersions.csproj" /><!-- Affectation de <PackageVersion> et <Version> -->

</Project>
